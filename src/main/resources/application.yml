spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Inventario_Final
    username: root
    password: julian
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
